<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchiveAndroid"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".feature.home.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.ArchiveAndroid" />
        <activity
            android:name=".feature.intro.view.IntroActivity"
            android:exported="true"
            android:theme="@style/Theme.ArchiveAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.home.recorddetail.view.RecordDetailActivity"
            android:exported="true" />
        <activity android:name=".feature.home.record.input.RecordInputActivity"
            android:exported="false" />
        <activity android:name=".feature.home.recommend.filter.RecommendFilterActivity"
            android:exported="false" />
        <activity android:name=".feature.home.recommend.detail.view.RecommendDetailActivity"
            android:exported="false" />
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="kakao.sdk">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="${kakaoScheme}" />
            </intent-filter>
        </activity>
    </application>

</manifest>